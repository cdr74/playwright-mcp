# flows/

Natural-language task specs, one per benchmarked flow. Each spec is given
**verbatim and identically** to both the MCP and CLI conditions — it's the
controlled variable in the experiment. See `TODO.md` Phase 2.
